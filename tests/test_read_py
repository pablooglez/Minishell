# test_read_input.py
tests = [
    # Entrada vacía
    "",  # Resultado esperado: NULL o salida vacía

    # Entrada solo con espacios
    "    ",  # Resultado esperado: NULL o salida vacía

    # Entrada solo con tabs
    "\t\t",  # Resultado esperado: NULL o salida vacía

    # Combinación de espacios y tabs
    "  \t  \t ",  # Resultado esperado: NULL o salida vacía

    # Reemplazo de tabs por espacios
    "echo\tworld",  # Resultado esperado: "echo world"

    # Eliminación de espacios adicionales
    "echo    multiple    spaces",  # Resultado esperado: "echo multiple spaces"

    # Comando con espacios al inicio y al final
    "   echo hello   ",  # Resultado esperado: "echo hello"

    # Comando complejo con espacios extra
    "ls    -la    |    grep    test",  # Resultado esperado: "ls -la | grep test"

    # Entrada solo con un salto de línea
    "\n",  # Resultado esperado: NULL o salida vacía

    # Entrada con EOF (Ctrl+D)
    "\x04",  # Resultado esperado: NULL o salida vacía

    # Entrada no vacía agregada al historial
    "echo hello",  # Resultado esperado: Comando agregado al historial

    # Entrada vacía no agregada al historial
    "    ",  # Resultado esperado: No se agrega al historial

    # Mostrar el prompt con directorio actual
    "pwd",  # Resultado esperado: Muestra el directorio actual antes del comando
]
